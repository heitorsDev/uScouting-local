import { useState } from "react"
import { useEffect } from "react"
export default function CreateAcc({ func, name, team, teamnumber, country}){

    return (
        <>
            <button onClick={func}>func</button>
        
        </>
    )
}